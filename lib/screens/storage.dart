import 'package:flutter/material.dart';

class StorageScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("내 보관소"),
      ),
      body: Center(
        child: Text("내 보관소 페이지"),
      ),
    );
  }
}
